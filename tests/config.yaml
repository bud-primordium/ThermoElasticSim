# config.yaml
unit_cell:
  a: [1.0, 0.0, 0.0]
  b: [0.0, 1.0, 0.0]
  c: [0.0, 0.0, 1.0]

interaction:
  potential_type: 'EAM'  # 或 'LennardJones'
  parameters:
    # EAM 参数或 Lennard-Jones 参数
    epsilon: 1.0
    sigma: 1.0
  cutoff: 2.5

optimizer:
  method: 'ConjugateGradient'  # 或 'NewtonRaphson'

deformation:
  delta: 0.01

stress_calculator:
  type: 'EAM'  # 或 'LennardJones'
